#include<stdio.h>
#include<conio.h>
int main()
{
	 int t;
	 scanf("%d",&t);
	 if(t<0)
	     printf("freezing");
	 else
	 
	 	if(t<0 && t<=20)
	 	  printf("cold");
	 	else
	 	
	 		if(t<21 && t<30)
	 		   printf("moderate");
	 		else
	 		
	 			if(t>30)
	 			   printf("hot");
	 			else
		        
		 
	 
	 return 0;
}
