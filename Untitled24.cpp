#include<stdio.h>
#include<conio.h>
int main()
{
	int n,d,r,x,count=0;
	scanf("%d%d",&n,&d);
	while(n>0)
	{
		r=n%10;
		if(d==x);
		{
			count++;
		}
		n=n/10;
	}
	         printf("%d",count);
}
